The first version I have sent will take the data from IvanescuChristian fork / repo.If there is no data it will search locally.
The second one will take it from the openmrs/openmrs-contrib-dependency-vulnerability-dashboard repo . If there is no data it will search locally.
I propose a version that will be fed the repositories where one would like to look then the app searches the repositories for the json files through a github api .

